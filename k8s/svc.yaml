apiVersion: v1
kind: Service
metadata:
  name: admins-svc
  labels:
    app: rekbers
    service: admins
spec:
  selector:
    app: rekbers
    service: admins
  ports:
    - name: http-port
      port: 3000
